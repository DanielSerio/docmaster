# Production Environment Variables
# Copy this file to .env.prod and update the values as needed
# IMPORTANT: Change all passwords and sensitive values before deploying to production!

# PostgreSQL Database
POSTGRES_USER=docmaster_prod
POSTGRES_PASSWORD=CHANGE_THIS_PASSWORD_NOW
POSTGRES_DB=docmaster_prod
DATABASE_URL=postgresql://docmaster_prod:CHANGE_THIS_PASSWORD_NOW@db-prod:5432/docmaster_prod?schema=public

# Server Configuration
NODE_ENV=production
